import { StyleSheet } from 'react-native';

export const homeBannerStyles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  image: {
    width: '100%',
    height: 210,
    borderRadius: 18,
  },
});

